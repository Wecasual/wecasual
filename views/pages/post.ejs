<% include ../partials/header.ejs %>
  <div class="container top-spacer">




      <aside class="callout rounded">
      <div class="text-vertical-center">
          <h1 class = "rounded blog-header"><%= post.title %></h1>
      </div>
  </aside>
  <p class="ml-3 mt-1 lead"><a class ="rounded blog-home button btn-secondary"href="/blog">&lt; Blog Home</a>
  <section id="portfolio" class="portfolio">
      <div class="container">
          <div class="row">
              <div class="col-lg-12 col-lg-offset-1 blog-body">
                  <p class="lead">By <%= post.author.first_name %> <%= post.author.last_name %> on <%= published.getMonth()+1 %>/<%= published.getDate() %>/<%= published.getFullYear() %></p>

                  <% if (post.categories.length > 0) { %>
                    in
                    <% post.categories.forEach(function(cat) { %>
                      <a href="/category/<%= cat.slug %>"><%= cat.name %></a>
                    <% }); %>
                  <% } %>

                  <br>
                  <hr>
                  <div class = "blog-body">
                    <%- post.body %>
                  </div>

              </div>
          </div>
      </div>
  </section>

</div>
<% include ../partials/footer.ejs %>
